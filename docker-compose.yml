services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aesthetico-dashboard
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./token.json:/app/token.json
    restart: unless-stopped
